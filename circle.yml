machine:
    services:
        - docker

compile:
    override:
        - make

deployment:
    hub:
        branch: master
        commands:
            - docker login -u $DOCKER_USER -p $DOCKER_PASS
            - make push
